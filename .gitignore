# KALLOS workspace root — applies to the whole repository

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Secrets & local environment
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local / machine-specific agent config (keep tracked examples without .local)
.mcp.json.local

# Evaluation harness outputs (mirrors evaluate/.gitignore)
evaluate/results/
evaluate/test_repos/
